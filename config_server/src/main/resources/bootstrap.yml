
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/manuelpravia/config_properties_microservice.git
          search-paths: /
          default-label: "main"